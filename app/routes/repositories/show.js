import Ember from 'ember';

export default Ember.Route.extend({
  // store: Ember.inject.service('store'),
  // model: function(params) {
  //   return this.get('store').find('repository', params.repository_id);
  // }
});
