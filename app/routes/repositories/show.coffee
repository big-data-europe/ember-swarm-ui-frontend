`import Ember from 'ember'`

RepositoriesShowRoute = Ember.Route.extend()
  # store: Ember.inject.service 'store'
  # model: (params) ->
  #   @get('store').find('repository', params.repository_id)


`export default RepositoriesShowRoute`
